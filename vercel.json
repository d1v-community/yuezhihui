{
  "installCommand": "pnpm install --no-frozen-lockfile",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/app$", "dest": "/app/index.html" },
    { "src": "/app/(.*)", "dest": "/app/index.html" }
  ]
}
